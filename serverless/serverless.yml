service: apollo-search-api

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: us-east-1
  memorySize: 128
  timeout: 10

functions:
  app:
    handler: app.handler
    events:
      - http:
          path: /api/search
          method: post
          cors: true
